# 3DGenerateFlow 前端工作流优化设计

> 参考 LibTV（liblib.tv）与 OiiOii（oiioii.ai）的产品设计，将原有“向导表单 + 节点工作流”升级为 **Lazy Canvas + 多视角网格 + 侧边 AI Agent 聊天** 的混合交互。

## 1. 调研结论

### LibTV 可借鉴点

- **Lazy Canvas / 无限画布 + 自动连线**：用户用自然语言描述需求，Agent 自动在画布上生成并连接节点，普通用户无需手动拖拽连线。
- **Skills 化工作流**：将专业流程封装成可复用的 Skill，降低重复配置成本。
- **故事板视图**：用 9/25 宫格预览故事节奏，提前发现不一致问题。
- **右侧聊天窗口**：实时用对话方式调整生成参数，而不离开画布。
- **自检机制**：自动识别生成缺陷并给出修复建议。

### OiiOii 可借鉴点

- **多 Agent 分工**：把复杂 pipeline 拆解为“美术导演 / 编剧 / 角色设计师 / 场景设计师 / 分镜师 / 音效导演”等角色，用户对流程有感知但不用操作每个细节。
- **Lazy Canvas**：宣称“无线画布控制，却不需要手动连节点”，自然语言即可让 AI 安排工作流。
- **素材库复用**：角色、场景一键加入素材库，后续剧集可复用，解决一致性痛点。
- **分镜网格生成**：用多宫格图一次性生成多个镜头，避免“抽卡”式反复生成。
- **单帧可重生成**：对不满意的某一分镜单独点击重生成，而不破坏整体。

## 2. 优化原则

1. **先对话/表单，后画布**：用户最低成本入口仍是上传 + 选择风格 + 一句话描述；画布作为可视化进度和高级调整层。
2. **无需手动节点连线**：画布只展示 AI 已安排好的步骤卡片，步骤之间自动用视觉连线表示依赖关系。
3. **关键中间产物可视化**：3D 生成前必须先展示多视角网格，让用户确认/修正视角，避免“开盲盒”。
4. **右侧 Agent 聊天常驻**：任何阶段都能用自然语言修改参数（如“底座加厚 2mm”“换成低多边形风格”）。
5. **素材/规则可复用**：后续支持保存风格、常用打印尺寸、壁厚规则为 Skill/模板。

## 3. 新的界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  Header: 3DGenerateFlow + 模式切换（Wizard / Canvas）       │
├──────────────┬──────────────────────────────┬───────────────┤
│  Left Panel  │        Middle Canvas         │  Right Panel  │
│  上传 + 参数  │  工作流步骤卡片 + 多视角网格   │  AI Agent 聊天 │
│  表单控制    │  + 3D 模型预览                 │  + 快捷操作    │
└──────────────┴──────────────────────────────┴───────────────┘
```

## 4. 组件拆分

- `UploadForm`：保留，但弱化显示，作为参数输入面板。
- `WorkflowCanvas`：Lazy Canvas，展示 6 个步骤卡片（上传 → 风格 → 多视角合成 → 3D 生成 → 打印检查 → 导出）。
- `StoryboardGrid`：4/6 宫格多视角预览，支持单张重生成与确认。
- `ChatPanel`：右侧聊天，支持快捷指令卡片。
- `ModelViewer`：保留，用于最终 3D 预览。

## 5. 状态映射

后端 Job 状态 → 前端步骤高亮：

| 后端状态 | 高亮步骤 | 说明 |
|----------|----------|------|
| pending | 上传 | 已提交，等待开始 |
| preprocessing | 风格/预处理 | 正在处理输入 |
| generating_multiview | 多视角合成 | 此时显示 StoryboardGrid |
| generating_3d | 3D 生成 | 合成完成后生成网格 |
| postprocessing | 打印检查 | 检查壁厚/流形 |
| completed | 导出 | 可下载 |
| failed | 当前步骤标红 | 显示错误 |

## 6. 下一步实现

1. 实现 `WorkflowCanvas`、`StoryboardGrid`、`ChatPanel`。
2. 重构 `App.tsx` 为三栏布局。
3. 扩展 Zustand store 支持聊天消息与视图模式。
4. 增加快捷操作：风格切换、底座加厚、重新生成某视角等（可先前端占位，后续接入后端指令）。
